# ADR 0003: Retrieval uses the host agent subscription

Accepted. AgentTraces search and MCP return compact, permissioned evidence with trace provenance. The Claude, Codex, Cursor, or other host already running for the user chooses tools and synthesizes an answer under that user's existing subscription. Cloud inference is a separate optional future feature and is not required for retrieval.

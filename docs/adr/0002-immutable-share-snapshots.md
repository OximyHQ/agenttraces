# External shares are immutable snapshots by default

An external share captures a redacted, permission-bounded snapshot of the selected overview, conversation, highlights, or full-trace view when it is created. This prevents an already-distributed capability URL from silently revealing later events; a live share is a separate explicit mode with the same revocation, expiry, and audit controls.

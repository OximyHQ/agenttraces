import Link from "next/link";
import { SiteHeader } from "@/components/site-header";

export default async function JoinPage({ params }: { params: Promise<{ token: string }> }) { const { token } = await params; return <main><div className="page-frame"><SiteHeader /><div className="join-layout"><section><p className="context-line">You have a team invitation</p><h1>Connect this machine to the team.</h1><p className="intro">Your coding sessions for team repositories will follow the owner’s visibility policy. You can pause capture or make any trace stricter.</p><ol><li><span>1</span><div><strong>Sign in or create your account</strong><p>This connects the device to your identity without moving existing personal traces.</p></div></li><li><span>2</span><div><strong>Run the setup command</strong><pre><code>npx agenttraces up --endpoint https://api.agenttraces.sh --team-token {token}</code></pre></div></li><li><span>3</span><div><strong>Review capture before enabling</strong><p>The installer lists detected sources, configured repositories, and the active team policy.</p></div></li></ol><div className="join-actions"><Link className="primary-link" href={`/sign-in?join=${encodeURIComponent(token)}`}>Continue to sign in</Link><Link href="/docs/teams">Read the team guide</Link></div></section><aside><h2>What the owner controls</h2><dl><div><dt>Default</dt><dd>Team policy</dd></div><div><dt>Repositories</dt><dd>Explicitly managed</dd></div><div><dt>Setup link</dt><dd>Scoped and revocable</dd></div></dl></aside></div></div></main>; }

import { DashboardShell } from "@/components/dashboard-shell";
import { demoTeam } from "@/lib/product-data";

export default function TeamPage() { return <DashboardShell section="team" preview><div className="page-heading"><div><p className="context-line">{demoTeam.name}</p><h1>Team and devices</h1></div><button className="text-action" type="button" disabled title="Connect the cloud API to create setup links">Create setup link · preview</button></div><div className="team-policy"><div><h2>Capture policy</h2><p>The owner decides the default for team work. Members can always choose stricter privacy.</p></div><strong>{demoTeam.policy}</strong></div><section className="dashboard-section"><div className="section-heading"><h2>Members</h2><span>{demoTeam.members.length} people</span></div><div className="member-table" role="table" aria-label="Team members">{demoTeam.members.map((member) => <div role="row" key={member.email}><span className="member-avatar" aria-hidden="true">{member.name[0]}</span><span role="cell"><strong>{member.name}</strong><small>{member.email}</small></span><span role="cell">{member.role}</span><span role="cell"><strong>{member.device}</strong><small>{member.seen}</small></span><button type="button" disabled title="Member controls are unavailable in preview" aria-label={`Manage ${member.name} unavailable in preview`}>•••</button></div>)}</div></section><section className="dashboard-section"><div className="section-heading"><h2>Team repositories</h2><button type="button" disabled title="Repository controls are unavailable in preview">Add repository · preview</button></div><p className="page-description">Owners and admins may inspect private traces only when the trace belongs to a repository listed here.</p><div className="repository-list">{demoTeam.repositories.map((repository) => <div key={repository}><span aria-hidden="true">◇</span><strong>{repository}</strong><small>GitHub · team-owned</small></div>)}</div></section><section className="dashboard-section"><div className="section-heading"><h2>Reusable setup link</h2><span>Illustrative only</span></div><div className="setup-link"><code>https://agenttraces.dev/join/at_join_••••••••••</code><button type="button" disabled>Copy · preview</button><button type="button" disabled>Revoke · preview</button></div><p className="page-description">The raw token is shown once. AgentTraces stores only its hash and records each redemption.</p></section></DashboardShell>; }

import Link from "next/link";
import { DashboardShell } from "@/components/dashboard-shell";
import { traces } from "@/lib/agenttraces-api";
import { demoTraces, type TraceRecord } from "@/lib/product-data";
import { ProviderIcon } from "@/components/provider-icon";

export default async function Dashboard() { const data = await traces(); const records = data.preview ? demoTraces : data.records as TraceRecord[]; return <DashboardShell section="overview" preview={data.preview}><div className="page-heading"><div><p className="context-line">Workspace overview</p><h1>Recent agent work</h1></div><Link className="text-action" href="/docs/teams">Add teammates</Link></div><dl className="summary-strip"><div><dt>Sessions</dt><dd>38</dd><small>last 7 days</small></div><div><dt>Active devices</dt><dd>3</dd><small>all healthy</small></div><div><dt>Pull requests</dt><dd>6</dd><small>with linked traces</small></div><div><dt>Reported usage</dt><dd>1.8m</dd><small>tokens · mixed accuracy</small></div></dl><section className="dashboard-section"><div className="section-heading"><h2>Latest traces</h2><Link href="/dashboard/traces/tr_01K2X9W4C7Q2">View trace detail →</Link></div><div className="trace-table" role="table" aria-label="Recent traces">{records.map((trace) => <Link className="trace-table-row" role="row" href={`/dashboard/traces/${trace.id}`} key={trace.id}><span className="source-mark" data-source={trace.source.toLowerCase().replaceAll(" ", "-")}><ProviderIcon source={trace.source} /></span><span role="cell"><strong>{trace.title}</strong><small>{trace.repository} · {trace.owner}</small></span><span role="cell">{trace.source}</span><span role="cell">{trace.updated}</span>{trace.pullRequests.length ? <b role="cell">#{trace.pullRequests[0]!.number}</b> : <b role="cell">—</b>}</Link>)}</div></section><section className="dashboard-section"><div className="section-heading"><h2>Ask your traces</h2><Link href="/docs/search">Search guide →</Link></div><div className="query-panel"><code>agenttraces search &quot;what did we learn about the queue timeout?&quot;</code><p>The same search is available to Claude Code, Codex, Cursor, and any MCP-compatible agent after setup.</p></div></section></DashboardShell>; }
